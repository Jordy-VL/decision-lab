# First full CE attempt: cancelled before checkpoint

Run: `ce-20260922-170803`. Code: `c91315bb9b9eb9127c58baf70b821ea2bdff9848`.
[Modal application](https://modal.com/apps/jordy-vlan/main/ap-OO22BxiK87M5jisChJeEOI).

## Observed outcome

Modal reported a cancellation signal and terminated the function. The last retained progress message is update 3,700 of 3,894 (95.0% of scheduled updates). The precise completed update count can be higher because logging occurs every 100 updates. There is no final epoch-loss report, checkpoint, training completion file, development report or calibration report in the persistent run directory. Test was not accessed. No replacement job was launched.

The application is stopped. Its retained status JSON still says `running`; that field is stale, not evidence of a live run. The launcher catches ordinary exceptions, but cancellation did not pass through that handler, leaving the previous status in its final persistence operation. Modal's terminal cancellation report and the artifact inventory establish the outcome.

The cancellation reason is unresolved. The message says the function was cancelled by a user or failure; it does not identify which. The recorded elapsed time is 1,387.93 seconds (23.13 minutes), below both the 55-minute training subprocess limit and 75-minute function limit. Do not attribute this to those configured timeouts without additional evidence.

## Hardware and cost

The actual full-run device was an A100 SXM4 **80GB**, whereas the initial profile received a 40GB device. The generic `A100` request did not establish an exact memory-tier match. At the previously recorded 80GB rate of $2.4984/hour plus about $0.2222/hour for requested CPU/RAM, the recorded function duration corresponds to approximately **$1.05 compute**. This is an estimate, not billing evidence; startup, preparation and storage are additional. The exact charged total remains unverified.

## Follow-up before any rerun

- Confirm cancellation cause if Modal event history supplies it; do not infer user intent from the generic error.
- Save periodic recoverable checkpoints and persist them to the Volume. The present trainer saves only at epoch completion, so this attempt cannot resume from its observed 3,700 updates.
- Mark cancellation explicitly in the launcher and retain terminal evidence separately from in-process status.
- Request the intended A100 memory tier explicitly and verify the returned device.
- Account for this attempt and the preceding profile within the existing $10 total budget. Do not silently restart cancelled work.

Raw retained status and training log are included alongside this note. No model-quality result is available from this attempt.
